#include <stdio.h>
//allocate huge array
FILE * iFile, * oFile;

int main (int argc, char* argv[])
{
	iFile = fopen (argv[1],"r");
	oFile = fopen (argv[2],"w+");
	
	while line != NULL:
		fscanf (iFile,"%s",str1);
		fscanf(fp,"%d",&numberOfPeople); 
		
	fprintf (oFile,"%f %f %d\n",__CruiseControl_V13,__CruiseControl_V14,__CruiseControl_V15);
	
		fclose(iFile);
	fclose(oFile);
	return 0;
	// printf("hello world get fucked and die");
}
